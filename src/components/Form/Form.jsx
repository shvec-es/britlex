const Form = props => {
  return (
    <form>
      <label>
        <input placeholder="Enter Your E-mail" />
      </label>
      <button type="submit">Subscribe</button>
    </form>
  );
};

export default Form;
